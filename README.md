

## Running

#### Scrape data

```bash
$(cd pollstrs && poetry run -C .. maturin develop); poetry run scrape
```

#### Test a quick bit of function

```bash
$(cd pollstrs && poetry run -C .. maturin develop); poetry run try
```